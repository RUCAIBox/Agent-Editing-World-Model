"""Pre-built agent scaffolds.

The primary scaffold is :class:`SearchSWEAgent` — a unified coding agent with
optional web search capabilities, supporting all task types (SWE-bench,
BeyondSWE) via dynamic prompt routing.
"""
